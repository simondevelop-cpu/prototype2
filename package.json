{
  "name": "canadian-insights",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/backend",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  }
}
